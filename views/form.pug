extends layout

block content
  form(method="POST" action="/new")
    h2 Add new message
    label Text
      input(type="text" name="text" required)
    label User
      input(type="text" name="user" required)
    button Submit
